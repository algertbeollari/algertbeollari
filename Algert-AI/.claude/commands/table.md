---
description: Convert the given content into a markdown table
---

Convert the following into a well-structured markdown table. Choose sensible column headers based on the natural dimensions of the content (don't force a comparison table if the content isn't comparative). If the content doesn't lend itself to tabular form, say so and suggest a better format instead of forcing a bad table.

Content: $ARGUMENTS
