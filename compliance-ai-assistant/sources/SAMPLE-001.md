# SAMPLE-001: Placeholder source

This is a placeholder source, not a real regulatory extract. It exists only
so the retrieval pipeline has something to index on first run.

Replace this file with genuine, authorised source material, for example:
- DFSA Rulebook module extracts (GEN, AML, COB, PIB, CIR, etc.) copied from
  the current, in-force DFSA Rulebook (public source).
- Your Firm's own approved, non-personal Markdown policies (e.g. AML/CFT
  Business Risk Assessment, Compliance Manual sections) — only after
  confirming classification permits this environment.

Every source file must have a matching entry in manifest.json before it is
indexed. Files without a manifest entry are ignored by the retrieval layer.
